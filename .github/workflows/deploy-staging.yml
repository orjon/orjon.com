name: Deploy to Staging

on:
  push:
    branches: [staging]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to Droplet
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DROPLET_IP }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            cd /var/www/orjon.com/staging
            echo "Pulling latest changes..."
            git fetch origin staging
            git reset --hard origin/staging
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
            echo "Checking required Node.js version..."
            nvm use || nvm install
            echo "Using Node.js version: $(node -v)"
            npm ci
            echo "Running tests..."
            npm run test
            echo "Removing .next directory..."
            [ -f /tmp/warm-images-staging.pid ] && kill $(cat /tmp/warm-images-staging.pid) 2>/dev/null || true
            rm -f /tmp/warm-images-staging.pid
            rm -rf .next
            set -a && . ./.env.staging 2>/dev/null || true && set +a
            echo "Environment: $NEXT_PUBLIC_ENV"
            echo "Starting build..."
            npm run build || { echo "Build failed! Aborting deployment."; exit 1; }
            echo "Build succeeded. Reloading..."
            pm2 reload orjon-staging --update-env
            echo "Waiting for server to be ready..."
            for i in $(seq 1 30); do curl -s -o /dev/null -w "%{http_code}" ${ORIGIN} | grep -q 200 && break; sleep 2; done
            echo "Starting image cache warming in background..."
            nohup npm run warm-images > /tmp/warm-images.log 2>&1 &
            echo $! > /tmp/warm-images-staging.pid
            disown
